import React from "react"

const name = "randomOrder"

const JSXVariables = () =>(
<div className="containers">
    <h2>
    random Order:
    {math.floor(math.random()* 11) + 1}
    </h2>
</div>
);

//Export ??
